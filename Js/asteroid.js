class Asteroid{
    constructor(){
        this.posX = Math.round(random(-windowWidth,windowWidth))
        this.posX = Math.round(random(-windowWidth,windowWidth))
        this.width = Math.round(random(10,56))
        this.height = Math.round(random(10,56))
    }

    
}